# CabinCopilot-SLM

Fine-tuned on-device small language model that turns driver-monitoring signals into
safe, context-aware coaching. Full README with architecture, results, and reproduction
steps lands with the trained model — see `docs/design/` for the spec and
`docs/explainers/` for the build log.
